<h1>Show article</h1>
  <%= @article.title %>
  <%= @article.content %>
  <%= link_to "Edit", edit_article_path(@article)%>
  <%= link_to "delete", article_path(@article),
        method: :delete,
        data: { confirm: "Are you sure?" } %>
